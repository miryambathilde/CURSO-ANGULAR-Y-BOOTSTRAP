export interface Product {
 title?: string; //title siempre tiene que ser un string
 desc?: string; //desc siempre tiene que ser un string
 price?: number; //price siempre tiene que ser un number
 picture?: string; //picture siempre tiene que ser un string

}
